[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3.12 -m pip install -r requirements.txt"]

[start]
cmd = "python3.12 -m gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
